scalar DateTime

enum UserRole {
  REPORTER
  AGENT
}

type User {
  id: ID!
  name: String!
  email: String!
  role: UserRole!
  createdAt: DateTime!
}

type AuthPayload {
  token: String!
  user: User!
}